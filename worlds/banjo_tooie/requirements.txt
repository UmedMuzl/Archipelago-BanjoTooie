# No external dependencies. Banjo-Tooie now plugs into the shared "EmuLoader Client"
# (worlds/EmuLoader) via the n64_client_handler contract and no longer depends on the
# standalone emu-loader package.
